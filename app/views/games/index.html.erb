<h1>Welcome!</h1>

<p>
	Join an open game or select New Game to start your own
</p>

<ul>
	<% @open_games.each do |game| %>
		<li><%= link_to game.name, game_path(game, skip_validation: true, game: {black_player_id: current_user.id} ), method: :put %></li>
	<% end %>
</ul>